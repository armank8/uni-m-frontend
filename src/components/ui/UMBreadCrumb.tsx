import { Breadcrumb } from "antd";


export default function UMBreadCrumb({ items }: { items: { label: string; link: string; }[] }) {
    const 
    return (
        <Breadcrumb items={}>

        </Breadcrumb>
    )
}

